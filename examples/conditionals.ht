import stdlib

func main(console: Stream): Stream {
	let name = ReadLine(&console)

	if len(name) < 40 {
		print(&console, "Name is short, " + name)
	} else {
		print(&console, "Name is long: " + name)
	}

	print(&console, "After if statement")
	return console
}
